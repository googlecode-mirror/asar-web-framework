#!/usr/bin/env php
<?php
set_include_path(
  realpath(dirname(__FILE__) . '/../core' ) . PATH_SEPARATOR . 
  get_include_path()
);

require_once 'Asar.php';
$cli = new Asar_Utility_CLI;
$cli->execute($argv);
?>

